export default class Generate {

    public async generate(
        {
            text,
            output
        }:{
            text: string,
            output: string
        }
    ) {

    }

}